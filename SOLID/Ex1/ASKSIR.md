1. Can validation rules become class objects? If yes then we are returning errors so we must club every output of different class to check all the errors.
2. Can printing be of different outputs, like csv, pdf etc?
3. TextTable is kind of a formattor so shouldn't it be a child of interface
4. 